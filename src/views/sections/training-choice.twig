<section class="training-choice">
	<h1 class="title">Выберите свое обучение:</h1>
	<div class="cards">
		{% for person in trainingType %}
			<div class="card">
				<img src="./assets/images/training-type/{{person.img}}.jpg" alt="{{person.name}}" class="card__img">
				<div class="card__content">
					<h3 class="card__name">{{person.name}}</h3>
					<p class="card__description">{{person.description}}</p>
					<button class="btn btn--transparent btn--transparent-black btn--md">Выбрать и оплатить</button>
				</div>
			</div>
		{% endfor %}
	</div>
  <p class="training-choice__ps">Если у вас возникнут какие-либо вопросы в процессе оплаты, пожалуйста, свяжитесь с вашим менеджером или напишите нам на <a href="#" class="text text--underline text--violent">info@sayes.ru</a>.</p>
</section>
